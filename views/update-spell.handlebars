<html>
{{!-- <h1>Update {{spell.spell_name}}:</h1>
<form id="update-spell" action="/spell" method="post">
    spell name: <input type="text" name="spell_name" value="{{spell.spell_name}}"><br>
    element: <input type="text" name="lname" value="{{spell.element}}"><br>
    cost: <input type="number" name="cost" value="{{spell.cost}}"><br>
    damage: <input type="number" name="damage" value="{{spell.damage}}"><br>
    Homeworld: <select name="homeworld" id="planet-selector"> 
        {{#each planets}}
            <option value="{{id}}">{{name}}</option>
        {{/each}}
    </select><br>
    cost: <input type="number" name="cost" value="{{person.age}}"><br>
</form>
<button onclick="updateSpell({{spell.spell_id}})">Update</button>
<script defer>classSelect({{spell.class_id}});</script> --}}

<h4 class="header">Update {{spell.spell_name}}</h4>
<form id="update_spell">
    <label for="spell_id_update">Enter the ID of the spell to update:</label>
    <input type="number" name="spell_id_update">

    <div class="dropdown">
        <label for="class">Class:</label>
        <select name="class">
            {{#each class}}
                <option value='{{class_id}}'>{{class_id}}</option>
            {{/each}}
        </select>
    </div>
    <div class="dropdown">
        <label for="element">Change element:</label>
        <select name="element">
            <option value="Air">Air</option>
            <option value="Water">Water</option>
            <option value="Fire">Fire</option>
            <option value="Earth">Earth</option>
            <option value="Dark">Dark</option>
            <option value="Light">Light</option>
        </select>
    </div>

    <div class="input">
        <form>
            <label for="name">Change name: </label>
            <input type="text" name="name"><br>
        
            <label for="cost">Change cost [10-1000]: </label>
            <input type="number" name="cost"><br>
        
            <label for="damage">Change damage [0-10000]: </label>
            <input type="number" name="damage"><br>
        </form>
    </div>

    <input type="submit" value="Update Spell">
</form>
</html>
